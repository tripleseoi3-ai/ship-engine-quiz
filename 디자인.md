# 디자인.md — 시험 대비 사이트 모양 규칙

이 문서는 **판단 기준**이다. 새로 만들거나 고칠 때 이 문서에 적힌 값을 그대로 쓴다.
여기 없는 스타일을 새로 만들지 않는다. 바꾸고 싶으면 **먼저 이 문서를 고치고** 그다음 화면을 고친다.

규칙마다 `확인법`을 붙였다. 화면을 보고 그 문장대로 확인하면 지켰는지 아닌지 혼자 판정할 수 있다.

- 이 규칙이 적용된 곳: `app/exam-notebook.html`, `web/index.html`(배포본 `site/index.html`), `server/public/style.css`
- 기준 날짜: 2026-09-11
- 7~9장(목록·날짜·막대)은 플래너·할 일·학습 기록 화면을 만들면서 2026-09-11 에 덧붙였다.
  **새 색이나 새 글자 크기를 만들지 않고 1·2장의 값만 다시 썼다.**
- 10장(발표 화면)은 2일차 보고용 슬라이드를 만들면서 2026-09-12 에 덧붙였다. 같은 원칙이다.
  그 슬라이드는 이 저장소가 아니라 **자료 폴더의 `제출/`** 에 있다 — 사건 자료는 공개 저장소에 올리지 않는다.
  점검표가 10장 → **11장**으로 밀리고 11줄 → 12줄이 됐다.

---

## 1. 바탕색과 글자색

대비는 **흰 바탕 위 글자색의 명암 차이**를 뜻한다. 숫자가 클수록 잘 읽힌다.
휴대폰은 화면이 작고 밝은 데서 보는 일이 많아, **본문으로 쓰는 글자색은 4.5:1 이상**이어야 한다.

### 밝은 화면 (기본)

| 쓰는 곳 | 색 | 흰 바탕에서의 대비 |
|---|---|---|
| 페이지 바탕 | `#EEF1F5` (아주 연한 회색) | — |
| 내용 상자 바탕 | `#FFFFFF` | — |
| 상자 안 구역 바탕 | `#F7F9FB` | — |
| **본문 글자** | `#1C2530` (남색 섞인 검정) | **15.5:1** |
| 설명 글자 | `#55616F` (회색) | **6.3:1** |
| 곁 정보 글자 (날짜·건수) | `#66737F` (진한 회색) | **4.9:1** |
| 파란 강조·링크·주 버튼 바탕 | `#1B6FB8` | **5.2:1** |
| 상단 띠 바탕 | `#10335E` / `#0A2342` (남색) | 흰 글자로 10:1 이상 |
| 정답 | `#1B6E43` (초록) | **6.3:1** |
| 오답 | `#B5342B` (빨강) | **6.0:1** |
| 주의 | `#9C6B12` (황갈) | 4.5:1 이상 |
| 선·테두리 | `#D5DCE4` / `#E6EBF0` | 글자가 아니므로 대비 기준 없음 |

### 어두운 화면

보는 사람이 어두운 화면을 쓰면 아래 값으로 **같은 짝**을 바꿔 쓴다. 밝은 화면 값을 그대로 뒤집지 않는다.

| 쓰는 곳 | 색 |
|---|---|
| 페이지 바탕 | `#0D1419` |
| 내용 상자 바탕 | `#151E26` |
| 본문 글자 | `#E3E9EF` |
| 설명 글자 | `#9BA8B6` (7.0:1) |
| 곁 정보 글자 | `#8C99A6` (5.8:1) |
| 파란 강조 | `#64A9DF` |
| 정답 / 오답 | `#5BBE8B` / `#E77A72` |

> **확인법 ①** 휴대폰을 밝은 곳에서 들고 팔을 뻗은 거리에서 본다. 문제 문장과 해설이 다 읽히면 통과다.
> 흐릿해 보이는 글자가 있으면 그 글자는 `#66737F`보다 연한 색을 쓴 것이다. 규칙 위반이다.
>
> **확인법 ②** 회색 글자로 **읽어야 하는 내용**을 적었으면 위반이다. 회색은 날짜·건수·근거 표시처럼
> 못 읽어도 문제 푸는 데 지장 없는 곁 정보에만 쓴다.

---

## 2. 글자 크기

기준은 **본문 15px**이다. 여기서 위아래로만 움직인다. 중간 크기를 새로 만들지 않는다.

| 쓰는 곳 | 크기 | 굵기 |
|---|---|---|
| 사이트 제목 (상단 남색 띠) | 26px | 700 |
| 상자 제목 (`오답 노트` 같은 것) | 15px | 600 |
| **문제 문장** | 17px | 500 |
| 본문·보기·입력칸 | 15px | 400 |
| 해설·정답 안내·표 내용 | 13px | 400 |
| 날짜·건수·유형 배지 | 12px | 500 |

**숫자와 기호에만 쓰는 큰 크기 세 가지.** 여기 적힌 세 곳 말고 다른 데 쓰지 않는다.
글이 아니라 **숫자 한 덩어리**이기 때문에 커도 읽는 순서를 흐트러뜨리지 않는다.

| 쓰는 곳 | 크기 | 굵기 |
|---|---|---|
| 풀이 결과의 점수 (`7 / 10` 의 `7`) | 32px | 600 |
| 요약 숫자 (연속 학습일 · 남은 오답 건수 같은 것) | 22px | 600 |
| OX 문제의 `O` `X` 보기 | 20px | 500 |

- **12px보다 작은 글자는 어디에도 쓰지 않는다.**
- 12px은 배지와 날짜에만 쓴다. **읽어야 하는 글은 13px 이상이다.**
- 문제 문장을 본문(15px)과 같은 크기로 두지 않는다. 문제가 제일 커야 눈이 먼저 간다.

> **확인법** 문제 화면을 열었을 때 **문제 문장이 그 화면에서 가장 큰 글자**인가.
> 상자 제목이나 버튼 글자가 문제 문장보다 크면 위반이다.

---

## 3. 문제 한 개가 차지하는 영역

**한 화면에 문제는 하나만 둔다.** 10문항을 한 페이지에 쭉 늘어놓지 않는다.

문제 하나는 흰 상자 하나를 차지하고, **안쪽 순서를 바꾸지 않는다.**

```
┌─────────────────────────────────────┐
│ ▌문제 풀이                 자동 출제 │  ← 제목 바 (회색 바탕, 왼쪽에 파란 세로 막대)
├─────────────────────────────────────┤
│ 3 / 10   ▓▓▓▓▓░░░░░░░░░   정답 2    │  ← 진행 줄
├─────────────────────────────────────┤
│ [객관식]                             │  ← 유형 배지
│                                     │
│ 다음 설명에 해당하는 것은?             │  ← 문제 문장 (17px)
│ 「육지의 물표를 관측해 …」             │
│                                     │
│ 1. 지문항법                          │  ← 보기 (세로로 쌓는다)
│ 2. 천문항법                          │
│ 3. 전파항법                          │
│ 4. 추측항법                          │
│                                     │
│ ┌─ 정답입니다 ──────────────────┐    │  ← 채점 결과 (푼 뒤에만 나온다)
│ │ 정답 · 지문항법                │    │
│ └───────────────────────────────┘    │
│                                     │
│ [다음 문제]                          │  ← 버튼은 맨 아래 왼쪽
└─────────────────────────────────────┘
```

- 상자: 흰 바탕, **1px 테두리**, 모서리 반지름 **3px**. 그림자 없음.
- 안쪽 여백: 위아래 좌우 **14px 이상**.
- 보기는 **세로로 쌓는다.** 가로로 늘어놓지 않는다. 보기 사이 간격 6px.
- 보기 한 칸의 높이는 **40px 이상.** 휴대폰에서 손가락으로 눌러야 한다.
- OX 문제만 예외로 O와 X를 가로로 둘 다 놓고, 각 칸이 폭을 절반씩 나눈다.
- 긴 문제 문장과 긴 보기는 **줄바꿈해서 다 보이게 한다.** 잘라서 `…`로 감추지 않는다.
- 채점 결과 상자는 **문제 아래에 붙인다.** 따로 떠 있는 창으로 띄우지 않는다.

> **확인법** 문제를 하나 풀어 본다. 위에서 아래로 `유형 → 문제 → 보기 → 채점 결과 → 버튼` 순서로
> 눈이 한 번만 내려가면 통과다. 눈이 위로 되돌아가야 하면 순서가 틀린 것이다.

---

## 4. 정답일 때와 틀렸을 때의 표시

**색만으로 알리지 않는다.** 색을 구분하기 어려운 사람도 있고, 밝은 햇빛 아래서는 색이 잘 안 보인다.
그래서 **글자 · 색 · 위치 세 가지를 동시에** 쓴다.

| | 정답일 때 | 틀렸을 때 |
|---|---|---|
| 글자 | `정답입니다` | `오답입니다` |
| 결과 상자 머리 | 연한 초록 바탕 `#E7F3EC` + 초록 글자 `#1B6E43` | 연한 빨강 바탕 `#FBEAE8` + 빨강 글자 `#B5342B` |
| 고른 보기 | 초록 테두리 + 연한 초록 바탕 | 빨강 테두리 + 연한 빨강 바탕 |
| 정답 보기 | (같음) | **초록 테두리로 따로 표시한다** |
| 반드시 보여줄 것 | `정답 · 지문항법` | `정답 · 지문항법` + `제출한 답 · 천문항법` |

- 틀렸을 때 **정답을 반드시 글자로 적어 준다.** 색칠만 하고 넘어가지 않는다.
- 제출한 답은 **취소선**을 그어 빨강으로, 정답은 **굵게** 초록으로 적는다.
- 주관식과 빈칸은 결과 상자에 **채점 기준을 함께 적는다** — `띄어쓰기와 조사 차이는 정답으로 처리합니다`.
- 근거가 있으면 `근거 · 지문항법 — 육지의 물표를 …` 한 줄을 덧붙인다.
- 오답 노트의 표에서도 같은 색 짝을 쓴다. 제출한 답은 빨강 취소선, 정답은 초록 굵게.

> **확인법** 화면을 흑백으로 만들어 본다(휴대폰 색약 모드나 흑백 인쇄).
> 그래도 맞았는지 틀렸는지 **글자만 읽고** 알 수 있으면 통과다. 색이 빠지면 알 수 없으면 위반이다.

### 아직 채점하지 않았을 때 (모의고사)

모의고사는 끝까지 풀고 **한꺼번에** 채점한다. 그래서 푸는 동안에는 맞았는지 틀렸는지 알려 주지 않는다.
이때 "내가 고른 것"을 표시해야 하는데, **여기에 초록을 쓰면 안 된다.**
초록은 이 문서에서 **정답**에만 쓰기로 했다. 채점 전에 초록을 칠하면 고른 답이 정답이라는 뜻이 된다.

| | 고른 보기 | 고르지 않은 보기 |
|---|---|---|
| 테두리 | 파랑 `#1B6FB8` | 회색 `#D5DCE4` |
| 바탕 | `#F7F9FB` | `#FFFFFF` |
| 글자 | 오른쪽에 **`고름`** | 없음 |

- 진행 줄에는 **`푼 문항 7`** 이라고 적는다. **`정답 7` 이라고 적지 않는다** — 아직 채점하지 않았다.
- 정답·오답은 제출한 뒤 **문항별 채점표**에서 `정답`/`오답` 글자로 알린다.

> **확인법** 모의고사를 풀다가 보기를 하나 고른다. **초록이 보이면 위반이다.**
> 그리고 진행 줄을 본다. `정답 N` 이 적혀 있으면 위반이다.

---

## 5. 버튼 모양과 위치

### 모양

| 종류 | 언제 쓰나 | 모양 |
|---|---|---|
| **주 버튼** | 그 화면에서 할 일 하나 (`제출`, `다음 문제`, `등록하고 자료 보기`) | 파란 바탕 `#1B6FB8` + 흰 글자, 굵기 500 |
| 보조 버튼 | 다른 선택 (`취소`, `모르겠습니다`, `오답 노트`) | 흰 바탕 + 회색 테두리 `#D5DCE4` + 검정 글자 |
| 글자 버튼 | 자주 안 쓰는 일 (`과목 삭제`, `오답 전체 삭제`) | 테두리·바탕 없음, 파란 글자 |

- 모두 **직사각형**, 모서리 반지름 **3px**. 둥근 알약 모양을 쓰지 않는다.
- 높이 **36px 이상**, 좌우 안쪽 여백 **14px**. 휴대폰에서 눌릴 크기여야 한다.
- 글자 크기 13px, 굵기 500. 버튼 글자를 문제 문장보다 크게 하지 않는다.
- 버튼 글자는 **누르면 일어나는 일**을 쓴다. `확인`·`OK` 같은 말 대신 `삭제`, `제출`, `전송하고 출제`.

### 위치

- **주 버튼은 한 화면에 하나만.** 파란 버튼이 둘 이상이면 어느 걸 눌러야 하는지 알 수 없다.
- 버튼은 그 일을 하는 상자의 **맨 아래, 왼쪽부터** 놓는다. 가운데 정렬하지 않는다.
- 왼쪽이 주 버튼, 그 오른쪽이 보조 버튼, 맨 오른쪽이 글자 버튼 순서.
- **확인 창(모달)만 예외** — 오른쪽 아래에 놓고 `취소`를 왼쪽, 실행 버튼을 오른쪽에 둔다.
- 지우는 버튼은 **바로 실행하지 않는다.** 확인 창을 한 번 띄우고, 그 창에서 무엇이 사라지는지 적는다.
- 휴대폰에서 버튼이 두 개 이상이면 **줄이 바뀌어 아래로 내려가게** 한다. 겹치거나 잘리면 위반이다.

> **확인법** 아무 화면이나 열고 **파란 버튼을 센다.** 두 개 이상이면 위반이다.
> 그리고 휴대폰 폭으로 좁혀 본다. 버튼이 화면 밖으로 나가거나 서로 겹치면 위반이다.

---

## 6. 쓰지 말 것

### 애니메이션

- **나타나기 · 미끄러지기 · 커지기 금지.** 스크롤을 내려야 글이 떠오르는 것도 금지다.
- 허용은 두 가지뿐이다: **진행 줄이 채워지는 것**, **기다릴 때 도는 동그라미**.
- 허용한 것도 **0.25초 이하**로 한다.
- 보는 사람이 기기에서 `동작 줄이기`를 켜 두었으면 **움직임을 전부 멈춘다.**

### 그림자 남발

- **그림자는 확인 창(모달) 하나에만 쓴다.** 그 창이 화면 위에 떠 있다는 걸 알려야 하니까.
- 상자 · 버튼 · 배지 · 표에는 그림자를 쓰지 않는다.
- 구역을 나누는 일은 **1px 테두리와 회색 바탕**이 한다. 그림자로 하지 않는다.

### 읽기 어려운 얇은 글씨

- **굵기 300 이하를 쓰지 않는다.** 본문 최소 굵기는 400이다.
- 큰 글자라고 얇게 쓰지 않는다. 제목은 700, 부제는 600이다.
- 연한 회색으로 본문을 쓰지 않는다. `#66737F`보다 연한 색은 곁 정보에만 쓴다.
- 글자에 투명도(`opacity`)를 걸어 연하게 만들지 않는다. 색을 직접 정한다.

### 그 밖에 하지 않는 것

- 12px보다 작은 글자
- 색만으로 정답·오답 알리기
- 한 화면에 문제 여러 개 늘어놓기
- 그라데이션, 사진 배경, 무늬 배경
- 화면 높이를 꽉 채우는 첫 화면 — 열면 바로 할 일이 보여야 한다
- 이모지를 구역 표시로 쓰기 (글자로 적는다)
- 표나 긴 글 때문에 **페이지 전체가 좌우로 흔들리는 것** — 좌우로 넘치는 것은 그 표 안에서만 밀리게 한다

---

## 7. 목록 한 줄과 체크 칸 (할 일 · 계획)

할 일과 계획은 **한 줄에 하나**씩, 위에서 아래로 쌓는다. 칸을 나눠 가로로 늘어놓지 않는다.

```
┌─────────────────────────────────────┐
│ ▌오늘 할 일                    3건   │  ← 제목 바
├─────────────────────────────────────┤
│ ☐  항해학 3장 표 외우기          삭제 │  ← 줄 하나
│    항해학 · 오늘까지                 │  ← 곁 정보 (12px 회색)
├─────────────────────────────────────┤
│ ☑  해사법규 오답 20문항   완료   삭제 │  ← 끝낸 줄
│    해사법규 · 어제까지               │
└─────────────────────────────────────┘
```

- 줄 한 개의 높이는 **40px 이상**. 줄 사이는 1px 선(`#E6EBF0`)으로 나눈다. 그림자를 쓰지 않는다.
- 왼쪽이 체크 칸, 가운데가 내용, 오른쪽이 `삭제`(글자 버튼).
- **체크 칸은 18px 정사각형.** 테두리 1px `#D5DCE4`, 모서리 3px, 켜지면 파란색 `#1B6FB8`.
- 손가락으로 누르는 곳은 체크 칸 18px이 아니라 **줄 전체**다. 내용 글자를 눌러도 켜지고 꺼져야 한다.
- **끝난 줄을 색으로만 알리지 않는다** (4장과 같은 이유). 세 가지를 동시에 쓴다:
  1. 체크 칸이 켜진다
  2. 내용 글자에 **취소선**을 긋고 글자색을 `#66737F`로 낮춘다
  3. 오른쪽에 **`완료`** 라고 글자로 적는다
- 내용은 본문 15px, 그 아래 곁 정보(과목·마감일)는 12px `#66737F`. **곁 정보에 읽어야 하는 내용을 적지 않는다.**
- 목록이 비면 회색 한 줄로 **무엇을 하면 채워지는지** 적는다 — `아직 할 일이 없습니다. 아래에서 추가하세요.`

> **확인법** 화면을 흑백으로 만들어 본다. 끝난 줄과 안 끝난 줄이 **취소선과 `완료` 글자만으로** 구분되면 통과다.
> 체크 칸 색이 빠졌을 때 구분할 수 없으면 위반이다.

---

## 8. 날짜와 남은 날 (D-day)

- 날짜는 **`9.15 (월)`** 모양으로 적는다. `2026-09-15` 같은 기계 표기를 화면에 그대로 내지 않는다.
- 날짜 · 요일 · 남은 날은 전부 **곁 정보**다. 12px, `#66737F`, 고정폭 글꼴.
- **남은 날은 배지로 적는다.** 모양은 3장 유형 배지와 같다 (12px, 굵기 500, 둥근 테두리).

| 언제 | 글자 | 색 |
|---|---|---|
| 시험까지 8일 이상 | `D-12` | 테두리 `#D5DCE4` · 글자 `#55616F` |
| 시험까지 7일 이하 | `D-3` | 테두리·글자 `#B5342B` + 연한 빨강 바탕 `#FBEAE8` |
| 시험 당일 | `D-DAY` | 테두리·글자 `#B5342B` + 연한 빨강 바탕 `#FBEAE8` |
| 시험이 지남 | `지남` | 테두리 `#D5DCE4` · 글자 `#66737F` |

- **급한 것을 빨강으로만 알리지 않는다.** 숫자(`D-3`)가 글자로 함께 적혀 있어야 한다.
- 오늘 줄은 배경색을 바꾸지 않는다. 대신 날짜 글자를 파랑 `#1B6FB8`으로 하고 **`오늘`** 배지를 붙인다.
- 달력을 7칸 가로 격자로 그리지 않는다. **하루 한 줄씩 세로로 7줄** 쌓는다. 휴대폰에서 칸이 뭉개지기 때문이다.

> **확인법** 휴대폰 폭으로 좁힌다. 날짜 줄이 잘리거나 좌우로 밀리면 위반이다.
> 그리고 `D-3` 배지에서 빨강을 빼 본다. 숫자가 남아 있으면 통과다.

---

## 9. 숫자를 보여주는 막대 (학습 기록)

- 막대는 **가로로 눕힌다.** 세로 막대는 휴대폰에서 폭이 모자란다.
- 한 줄 = `이름 | 막대 | 숫자`. **숫자를 반드시 오른쪽에 글자로 적는다.** 막대 길이로만 알리지 않는다.
- 막대 색은 파랑 `#1B6FB8` 한 가지. 바탕 홈은 `#E6EBF0`. 높이 8px, 모서리 3px.
- **여러 색으로 칠하지 않는다.** 항목을 구분해야 하면 색이 아니라 줄을 나눠서 구분한다.
- 막대가 늘어나는 **움직임을 넣지 않는다.** 6장의 허용 두 가지에 막대는 없다.
- 값이 0인 줄도 지우지 않고 남긴다 — 안 한 날이 보여야 기록이다.

> **확인법** 막대를 전부 가려 본다. 오른쪽 숫자만 읽어도 같은 내용을 알 수 있으면 통과다.

---

## 10. 발표 화면 (보고용 슬라이드)

보고서를 읽게 하는 대신 **앞에 서서 말할 때** 쓰는 화면이다. 2026-09-12 에 덧붙였다.
**새 색이나 새 글자 크기를 만들지 않고 1·2장의 값만 다시 썼다.** 늘린 것은 하나뿐이다 —
숫자 전용 큰 크기를 쓰는 자리가 **세 곳에서 네 곳**이 됐다(아래 표의 굵은 줄).

### 한 장에 하나

- 3장 「한 화면에 문제는 하나만」과 같은 규칙이다. **한 장에는 말하려는 것 하나만 둔다.**
- 넘기는 것은 **좌우 방향키**와 화면 좌우 누르기. 스크롤로 넘기지 않는다.
- 장 번호와 전체 장수를 오른쪽 아래에 적는다. 12px 곁 정보다.

### 크기

발표 화면은 **읽는 화면이 아니라 보는 화면**이다. 손에 든 휴대폰이 아니라 **몇 미터 떨어진 자리에서**
본다. 그래서 2026-09-12 에 실제로 띄워 보고, **1·2장 값에 +5px 한 단계를 올린 표**를 따로 두기로 했다.
**비율은 그대로다.** 어느 글이 크고 어느 글이 작은지는 2장과 똑같이 유지된다.

| 쓰는 곳 | 크기 | 굵기 | 1·2장의 어느 값에서 왔나 |
|---|---|---|---|
| 장 제목 | **31px** | 700 | 사이트 제목 26px + 5 |
| 그 장에서 먼저 읽어야 하는 한 문장 | **22px** | 500 | 문제 문장 17px + 5 — 같은 자리다 |
| 장 부제 · 본문 · 표 머리 | **20px** | 600 / 400 | 상자 제목·본문 15px + 5 |
| 표 내용 · 설명 | **18px** | 400 | 해설 13px + 5 |
| 장 번호 · 근거 표시 | **17px** | 500 | 날짜·배지 12px + 5 |
| **한 장의 핵심 숫자** | **37px** | **600** | 점수 32px + 5 — **쓰는 자리를 네 번째로 늘렸다** |

- **+5 는 발표 화면에만 쓴다.** 1·2장(웹 화면)의 값은 그대로다. 두 표를 섞지 않는다.
- 여기 없는 크기를 새로 만들지 않는다. 더 키우고 싶으면 **이 표를 먼저 고친다.**
- 글자가 커진 만큼 **한 장에 들어가는 줄이 줄어든다. 줄을 줄이지 말고 장을 나눈다.**
  한 장의 표는 **여섯 줄을 넘기지 않는다.** 넘치면 그 장은 둘로 나눌 자리다.

핵심 숫자는 **한 장에 하나만** 쓴다. 두 개를 쓰고 싶으면 그 장을 둘로 나눈다.

### 빔프로젝터에서는 판을 통째로 키운다

**더 키울 일이 생기면 글자를 하나씩 손대지 않는다.** 위 표의 값은 그대로 두고
**장 전체를 같은 비율로 확대한다.** 그래야 크기 차이가 유지된다.
개별 글자를 따로 키우기 시작하면 2장의 크기 체계가 무너진다.

### 여백

- 장 안쪽 여백은 **위아래 20px · 좌우 26px**. 3장의 「14px 이상」을 지키되 그 이상 벌리지 않는다.
- **장 제목 위만 28px 을 더 띄운다.** 안쪽 여백까지 합쳐 판 위에서 48px 이다. 2026-09-12 에 띄워 보고 정했다 —
  20px 만으로는 제목이 흰 판에 **얹힌 게 아니라 위가 잘린 것처럼** 보였다. 아래쪽 20px 은 그대로 둔다.
  **제목 위에만 쓰는 값이다.** 다른 자리의 여백을 이 값으로 벌리지 않는다.
- **표의 옆 제목 칸과 본문 사이는 40px 벌린다.** 옆 제목은 굵은 글자라 10px 로는
  본문과 한 덩어리로 붙어 읽힌다. 24px 로 올려 봤지만 네 글자 이름이 들어간 줄은 그래도 붙어 보였다.
  2026-09-12 에 두 번 띄워 보고 40px 로 정했다.
  **줄 사이는 그대로 1px 선 하나다.** 가로만 벌리고 세로는 벌리지 않는다.
- 이 40px 은 **칸 너비가 아니라 안쪽 여백으로 준다.** 너비만 못 박으면 같은 표에서도
  긴 이름의 줄만 틈이 좁아진다 — 두 글자 이름은 78px 이 남는데 네 글자 이름은 34px 만 남아
  그 줄만 붙어 보였다. 여백으로 주면 이름이 길어질 때 **칸이 알아서 넓어져** 어느 줄이나 40px 이 남는다.
  줄마다 다른 너비를 주지 않는다.
- **칸이 셋 이상인 표는 칸 사이도 20px 벌린다.** 맨 오른쪽 칸의 바깥쪽은 벌리지 않는다.
  10px 일 때 가운데 칸의 긴 글이 오른쪽 칸에 16px 까지 붙었다.
- 그렇게 벌리면 가운데 칸이 좁아져 **글이 한 줄 더 넘어갈 수 있다.** 그때는 글을 줄이지 말고
  그 칸을 넓힌다. 오른쪽 칸에 남는 자리가 있으면 거기서 가져온다.
- **표 위의 무리 이름은 작은 제목으로 띄우지 말고 표 머리줄로 넣는다.** 표 밖에 두면
  이름이 이름 칸과 어긋나 보이고 표가 어디서 시작하는지도 흐리다. 머리줄로 넣으면
  이름 칸과 같은 줄에 서고 **아래 1px 선이 표의 시작을 알린다.** 오른쪽 머리 칸은 비워 둔다 —
  무리가 둘인 장에서 채우면 같은 말을 두 번 적게 된다. 2026-09-12 에 정했다.
- **이어지는 장들은 이름 칸 너비를 하나로 맞춘다.** 한 이야기를 나눠 담은 장들은 같은 값을 쓴다.
  장이 넘어갈 때 본문이 좌우로 움직이면 눈이 따라가느라 읽는 속도가 떨어진다.
- **한 장에 표가 둘이면 표와 표 사이를 16px 띄운다.** 9px 으로는 두 무리가 한 덩어리로 읽혔다.
  윗줄의 「세로는 벌리지 않는다」는 **한 표 안의 줄 사이** 이야기다. 표와 표를 가르는 자리는 벌린다.
  다만 **장의 첫 표 위는 띄우지 않는다.** 장 제목에서 두 번 내려온 것처럼 보인다.
- **남는 공간을 아래에 몰지 않는다.** 내용이 장을 다 못 채우면 **위아래로 나눠 가운데에 놓는다.**
  아래만 휑하게 비면 장이 끝난 것처럼 보여서, 뒤에 더 있는 줄 모른다.
- 그래도 **표가 절반도 못 채우면** 그 장은 **앞뒤 장과 합칠 자리**다. 억지로 늘리지 않는다.
- 표의 줄 사이는 1px 선(`#E6EBF0`) 하나로 나눈다. 줄마다 빈 칸을 더 넣지 않는다.

### 색

1장 값을 그대로 쓴다. 발표용이라고 새 색을 만들지 않는다.

- 바탕 `#EEF1F5` · 장 바탕 `#FFFFFF` · 글자 `#1C2530` · 설명 `#55616F` · 곁 정보 `#66737F`
- 강조는 파랑 `#1B6FB8` 하나. **한 장에 강조는 한 군데.** 5장 「파란 버튼은 화면당 하나」와 같은 이유다
- 판정에는 4장의 짝을 그대로 쓴다 — 확인된 것 `#1B6E43`, 어긋난 것 `#B5342B`, 주의 `#9C6B12`
- **색만으로 알리지 않는다.** 확인된 것에는 `확인`, 확인 못 한 것에는 `미확인` 을 **글자로** 적는다
- **표 위 소제목과 표의 이름 칸을 같은 색으로 두지 않는다.** 둘 다 굵기 600 이라 색이 같으면 한 덩어리로 보인다.
  소제목은 본문색 `#1C2530`, **표의 이름 칸은 설명색 `#55616F`** 로 한 단계 낮춘다.
  이름 칸은 무슨 항목인지 알려 주는 딱지이고, 읽어야 하는 내용은 그 오른쪽 칸이다.
- **표 머리는 줄바꿈시키지 않는다.** 「정상 (18:00~23:12 평균)」 이 「…평 / 균)」 으로 잘리면
  낱말이 두 동강 난다. 칸 너비를 늘리거나 말을 줄인다.

**발표 화면은 밝은 화면 값만 쓴다.** 1장의 어두운 화면 표에는 **선·테두리 색과 연한 초록·빨강 바탕이 없다.**
없는 값을 지어내서 어두운 화면을 만들지 않는다. 빔프로젝터로 쏘는 화면이라 밝은 쪽으로 고정한다.
어두운 화면이 필요해지면 **1장 표를 먼저 채우고** 그다음 만든다.

### 하지 않는 것

6장을 그대로 따른다. 발표라고 예외를 두지 않는다.

- 장이 넘어갈 때 **미끄러지거나 흐려지는 효과 금지**
- 그림자 · 그라데이션 · 사진 배경 금지
- **12px보다 작은 글자 금지** — 뒷자리에서 못 읽는다
- 이모지를 구역 표시로 쓰지 않는다

### 근거를 장 안에 적는다

- **숫자가 있는 장에는 그 숫자가 나온 파일 이름을 12px 로 함께 적는다.**
  완료기준.md 2장에서 해설마다 출처를 적게 한 것과 같다.
- 확인하지 못한 것은 **그 장에서** `미확인` 이라고 적는다. 뒤에 몰아서 한 번에 말하지 않는다.

> **확인법** 아무 장이나 연다. **숫자를 하나 고르고 그 장 안에서 파일 이름을 찾는다.**
> 찾을 수 있으면 통과다. 그리고 그 장의 32px 숫자를 센다. **둘 이상이면 위반이다.**

---

## 11. 인쇄본 (PDF)

문서를 종이나 PDF로 낼 때 쓴다. 2026-09-12 에 덧붙였다.
**발표 화면(10장)의 +5 표를 쓰지 않는다.** 인쇄본은 손에 들고 가까이서 읽으므로
**1·2장 값을 그대로** 쓴다. 즉 제목 26px, 문제 문장 자리 17px, 본문 15px, 표 13px, 곁 정보 12px.

- 색도 1장 **밝은 화면 값**만 쓴다. 종이에서 어두운 바탕은 쓰지 않는다.
- 종이는 **A4 세로**, 여백은 위아래 18mm · 좌우 16mm.
- **표가 페이지 사이에서 잘리지 않게** 한다. 넘칠 것 같으면 표 전체를 다음 쪽으로 넘긴다.
- 다만 **한 쪽보다 큰 표는 나눌 수밖에 없다.** 그때는 두 가지를 지킨다.
  1. **표 머리를 쪽마다 되풀이한다.** 둘째 쪽에서 각 칸이 무슨 열인지 모르면 안 된다.
  2. **줄 하나가 두 쪽에 걸치지 않게 한다.** 한 줄은 통째로 한 쪽에 들어간다.
- 제목 바로 아래에서 쪽이 바뀌지 않게 한다. 제목만 남고 내용이 넘어가면 안 된다.
- 그림자·움직임은 6장대로 쓰지 않는다. 인쇄물에는 어차피 남지 않는다.
- **링크는 글자 그대로 둔다.** 파랑 `#1B6FB8` 으로 적되 밑줄을 긋지 않는다. 종이에서는 눌리지 않으므로
  긴 주소는 본문에 늘어놓지 말고 **출처 표에만** 둔다.

### 문서의 뼈대 — 네 가지만 쓴다

보고 문서는 줄글만 늘어놓지 않는다. 아래 넷으로 짠다. **새 모양을 더 만들지 않는다.**

| 이름 | 언제 쓰나 | 모양 |
|---|---|---|
| **표지** | 문서 첫 쪽 | 제목 **31px**(10장 값을 다시 쓴다) · 그 아래 파란 2px 선 52px · 문서 정보는 13px 표. 표지만 쓰고 쪽을 넘긴다 |
| **장 제목** | `##` | 앞에 **번호 배지**를 붙인다. 24px 정사각형, 모서리 3px, 파랑 `#1B6FB8` 바탕에 흰 숫자 12px |
| **카드** | 인용·요약·주의 | 회색 바탕 `#F7F9FB` + 1px 테두리 `#E6EBF0` + 모서리 3px. 왼쪽에 파란 2px 세로 막대 |
| **번호 목록** | 요지·순서 | 숫자를 **원형 배지**로. 18px 원, 파란 바탕에 흰 숫자 12px. 글은 옆에 붙인다 |

- **표 머리에 연한 띠**를 깐다. 바탕 `#F7F9FB`, 아래에 1px 선 `#D5DCE4`. 머리 글자는 설명색 `#55616F`.
- 표의 **첫 칸(이름 칸)은 굵게 600**, 나머지는 400. 이름 칸과 본문 사이는 **12px** 이상 띄운다.
- 배지·막대에 쓰는 파랑은 **1장의 `#1B6FB8` 하나**다. 새 색을 만들지 않는다.
- 아이콘·그림은 쓰지 않는다. 6장의 「이모지를 구역 표시로 쓰지 않는다」와 같은 이유다.

> **확인법** 표지를 뺀 아무 쪽이나 편다. **장 제목 앞에 번호 배지가 있는가.**
> 그리고 문서 전체에서 파랑을 센다. `#1B6FB8` 말고 다른 파랑이 있으면 위반이다.

> **확인법** PDF 를 열어 아무 쪽이나 편다. **한 쪽에 들어갈 수 있는 표가 잘려 있으면 위반이다.**
> 잘린 표가 있으면 그 표가 한 쪽보다 큰지 본다. 크다면 **둘째 쪽에 표 머리가 있는지** 확인한다. 없으면 위반이다.
> 그리고 제목만 덩그러니 있고 내용이 다음 쪽으로 넘어간 자리가 있으면 위반이다.

---

## 12. 한 장 점검표

새로 만들거나 고친 뒤 이 열두 줄을 순서대로 본다. 하나라도 아니면 고치고 다시 본다.

1. 휴대폰 폭으로 좁혔을 때 좌우로 흔들리지 않는가
2. 문제 문장이 그 화면에서 가장 큰 글자인가
3. 파란 버튼이 화면당 하나인가
4. 흑백으로 봐도 맞았는지 틀렸는지 글자로 알 수 있는가
5. 틀렸을 때 정답이 글자로 적혀 있는가
6. 그림자가 확인 창 말고 다른 곳에 있지 않은가
7. 읽어야 하는 글이 회색(`#66737F`보다 연한 색)으로 적혀 있지 않은가
8. 모의고사를 푸는 동안 고른 보기에 **초록이 보이지 않는가** (초록은 정답에만 쓴다)
9. 끝낸 할 일이 취소선과 `완료` 글자로도 구분되는가 (체크 칸 색만으로 알리고 있지 않은가)
10. 남은 날이 `D-3`처럼 숫자로 적혀 있는가 (빨강으로만 알리고 있지 않은가)
11. 막대 오른쪽에 숫자가 글자로 적혀 있는가
12. 발표 화면이라면 — 한 장에 32px 숫자가 하나뿐이고, 숫자가 나온 파일 이름이 그 장에 적혀 있는가
