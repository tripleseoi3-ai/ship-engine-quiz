# 히스토리.md — 시험 대비 앱이 어떻게 여기까지 왔나

이 문서는 **무슨 일이 있었는지 적는 곳**이다. 모양 규칙은 [디자인.md](디자인.md)에,
다 된 것으로 보는 조건은 [완료기준.md](완료기준.md)에 있다.

- 기준 날짜: 2026-09-11 ~ 09-12 (1일차와 2일차)
- 저장소: https://github.com/tripleseoi3-ai/ship-engine-quiz
- **깨진 것을 지우지 않는다.** 실패한 배포와 잘못 나갔던 문구도 아래에 그대로 남겼다.
  지우면 같은 실수를 반복한다.

---

## 1. 지금 열리는 주소

| 무엇 | 주소 | 어떻게 고치나 |
|---|---|---|
| **시험노트 (전체 앱)** | https://ship-engine-quiz.vercel.app | `web/` 을 고치고 `main` 에 push → Vercel 이 다시 배포 |
| 미리 만들어 둔 20문항 | https://ship-engine-quiz.vercel.app/20.html | 프로젝트 루트의 `시험대비.html` 을 고친다 |
| 쌓아가는 시험노트 (📚) | https://claude.ai/code/artifact/59f8e806-2775-4bb6-bb29-530217c28aa0 | `app/exam-notebook.html` — **초기 단일 파일 버전. 이번 작업으로 바뀌지 않았다** |
| 항해·법규·기관 문제풀이 (⚓) | https://claude.ai/code/artifact/c07388d5-e3f6-4a82-b2ba-cae296f22105 | `시험대비.html` 의 게시본 — **이번 작업으로 바뀌지 않았다** |

> **주소가 네 개인 이유.** 위 두 개는 **Vercel 에 올린 웹사이트**이고, 아래 두 개는 **Claude 게시물**이다.
> 서로 다른 것이다. 헷갈리면 엉뚱한 쪽을 고치게 된다. 지금 쓰는 것은 **맨 위 시험노트**다.

두 번 눌러 바로 여는 파일도 있다 — `시험대비.html`, `app/exam-notebook.html`.
서버도 인터넷도 필요 없다.

---

## 2. 만들어 온 순서

아래 열한 줄 가운데 **열은 2026-09-11 하루에** 일어났고, **마지막 하나가 2일차(09-12)** 다.

| 시각 | 커밋 | 무엇이 들어왔나 |
|---|---|---|
| 15:59 | `611aaa1` | 인터뷰 → 골 → 명세 기록(`docs/specs/`), 단일 파일 앱 `app/exam-notebook.html`, **모양 규칙 `디자인.md`** |
| 16:26 | `5ef1147` | 학습자료에서 뽑은 고정 20문항 `시험대비.html`, 판정 기준 `완료기준.md` |
| 16:59 | `e1821ed` | 프론트/백엔드를 나눈 로컬 서버 앱 (`server/`). **출제·채점 규칙이 `server/lib/study.js` 한 곳으로 모였다** |
| 17:32 | `0fb99db` | Vercel 배포 설정 (`vercel.json`, `.vercelignore`, `scripts/build.mjs`) |
| 18:04 | `2fa586e` | 사이트에 전체 앱 올리기 — 과목 추가 · 출제 · 오답노트 (`web/`) |
| 19:05 | `aa63734` | **오늘 · 플래너 · 할 일 · 학습 기록 화면, 복습 주기, 모의고사** |
| 19:10 | `7cc8c5f` | 배포 실패 고침 (아래 4장) |
| 19:28 | `c01d6ce` | 배포본을 열어 점검하다 찾은 두 가지 고침 (아래 5장) |
| 19:35 | `9de77bb` | 이 문서 `히스토리.md` |
| 22:53 | `a2fce6a` | **2일차준비.md** — 밤에 받아 둘 것과 이튿날 시간표 (아래 7장) |
| 09-12 | `4f92cfd` | **디자인.md 10장 「발표 화면」** — 슬라이드를 만들기 전에 규칙부터 (아래 7장) |

---

## 3. 2026-09-11 마지막 작업 — 화면 넷과 복습 주기

사용자 요청은 **과목추가 · 문제 생성 · 오답 노트 · 학습내용 요약 · 플래너 · 투두리스트**,
그리고 *더 들어가면 좋을 것을 넣어 달라* 였다.

앞의 넷은 이미 있었다. **플래너와 할 일을 새로 만들고**, 아래 여섯을 더 넣었다.
전부 **"시험 날짜에서 거꾸로 계산해 오늘 뭘 할지 알려 준다"** 한 줄로 이어지는 것만 골랐다.

| 더 넣은 것 | 왜 |
|---|---|
| **오늘 화면** (첫 화면) | 열자마자 오늘 복습·할 일·계획·D-day가 보인다. 플래너와 할 일이 따로 놀지 않게 묶는 자리 |
| **과목별 시험일 + D-day** | 플래너의 기준점. 없으면 플래너가 그냥 메모장이 된다 |
| **복습 주기 (1·3·7·14일)** | 오답을 한 번 맞혔다고 빼지 않는다. 오답 노트가 실제 효과를 내는 부분 |
| **약점 분석** | 어느 **절**에서 자주 틀리는지. 이미 저장하던 출처 정보를 그대로 쓴다 |
| **학습 기록** | 연속 학습일 · 최근 14일 푼 문항 · 회차별 점수 |
| **모의고사** | 20문항을 중간 채점 없이 시간 재며. 끝에 문항별 채점표 |

덤으로 **시험일까지 자동으로 계획 나누기**(자료의 절을 남은 날에 배분), **인쇄**,
**키보드 단축키**(보기 `1`~`4`, OX 는 `O`·`X`)가 들어갔다.

### 복습 주기가 도는 방식

```
틀림          → 오답 노트에 넣고, 다음 날 다시 띄운다        (연속 맞힘 0)
다시 맞힘     → 사흘 뒤                                    (연속 맞힘 1)
또 맞힘       → 이레 뒤                                    (연속 맞힘 2)
또 맞힘       → 열나흘 뒤                                  (연속 맞힘 3)
또 맞힘       → 다 외운 것으로 보고 오답 노트에서 뺀다        (연속 맞힘 4)
한 번이라도 틀리면 → 처음으로 되돌린다                        (연속 맞힘 0)
```

### 저장

- 예전 저장본(과목만 있던 v1)을 **과목 · 계획 · 할 일 · 학습 기록(v2)** 으로 올린다. 예전 데이터를 버리지 않는다.
- 내보내기 파일 하나에 **복습 단계와 다음 복습일까지** 함께 들어간다.
- 저장 위치는 그대로 **접속한 사람의 브라우저 안**이다. 서버로 전송하지 않는다.

### 모양 규칙을 먼저 고쳤다

`디자인.md` 는 참고 자료가 아니라 **판단 기준**이다. 그래서 화면보다 **규칙을 먼저** 고쳤다.

- 7장 **목록 줄과 체크 칸** — 할 일·계획 한 줄의 생김새. 끝난 줄은 체크 색이 아니라 **취소선과 `완료` 글자**로 구분한다
- 8장 **날짜와 남은 날** — `9.15 (월)` 표기, `D-3` 배지. 달력을 7칸 격자로 그리지 않고 **하루 한 줄씩 세로로** 쌓는다
- 9장 **막대** — 가로 막대 + **오른쪽에 숫자를 글자로**. 막대 길이로만 알리지 않는다
- 2장에 **숫자 전용 큰 크기 세 개**(32·22·20px)를 적었다. 새로 만든 값이 아니라 이미 코드에 있었는데 문서에 없던 값이다
- 점검표가 7장 → **10장**으로 밀리고 7줄 → 10줄이 됐다 (나중에 11줄)

---

## 4. 깨져 있었던 것 ① — 배포가 두 번 연속 실패하고 있었다

**증상.** `main` 에 push 하고 주소를 열었더니 **예전 20문항 페이지**가 그대로 나왔다.
push 는 성공했으니 아무도 몰랐다.

| 커밋 | Vercel |
|---|---|
| `0fb99db` Vercel 배포 설정 추가 | 성공 ← **이게 계속 떠 있던 것** |
| `2fa586e` 사이트에 전체 앱 올리기 | **실패** |
| `aa63734` 플래너·할 일 추가 | **실패** |

즉 **그 전 세션의 "전체 앱 올리기"도 실제로는 배포된 적이 없었다.**

**원인.** `.vercelignore` 가 `server/` 를 통째로 업로드에서 뺐는데,
`scripts/build.mjs` 는 `server/lib/study.js` 와 `server/public/style.css` 를 읽는다.
그 규칙은 `site/` 에 20문항 페이지 하나만 있던 시절에 쓰였고, 전체 앱이 들어오면서 맞지 않게 됐다.

**어떻게 확인했나.** Vercel 로그인이 없어 빌드 로그를 직접 읽을 수 없었다.
그래서 **`.vercelignore` 가 빼는 것을 그대로 뺀 사본**을 만들어 거기서 빌드를 돌렸다.

```
고치기 전 → 빌드 실패: 파일이 없습니다: server/lib/study.js
고친 뒤   → 빌드 완료, site/ 에 5개 파일
```

**고친 것.** `server/` 통째 제외를 지우고 개인 기록인 `server/data/` 만 뺀다.
`.vercelignore` 맨 위에 **빌드가 읽는 파일 여섯 개**를 적어 두었다.

> **여기서 배운 것 — push 했다고 배포된 것이 아니다.**
> ```bash
> gh api repos/tripleseoi3-ai/ship-engine-quiz/commits/<sha>/status --jq '.statuses[] | {state, description}'
> ```

---

## 5. 깨져 있었던 것 ② — 배포본을 열어 보고 찾은 두 가지

배포가 된 뒤, **저장소를 비우고 처음 들어온 사람처럼** 주소를 열어 화면을 하나씩 짚었다.
그러다 둘을 찾았다.

### (1) 복습 안내가 거짓말을 했다

한 번도 틀린 적 없는 문제를 맞혀도 이렇게 나왔다.

```
복습 · 4번 연속으로 맞혀 오답 노트에서 뺐습니다.
```

`reviewNote` 가 **"오답 노트에 그 문항이 없다"** 는 사실 하나만 보고 졸업으로 단정했는데,
**애초에 들어간 적이 없는 경우**와 구별하지 못했다.

이제 무슨 일이 일어났는지를 그대로 적는다.

| 무슨 일 | 화면에 나오는 글 |
|---|---|
| 처음부터 맞힘 | `복습 · 처음부터 맞혔습니다. 오답 노트에 넣지 않습니다.` |
| 새로 틀림 | `복습 · 오답 노트에 넣었습니다 · 다음 복습일 9.12 (토)` |
| 다시 맞힘 | `복습 · 연속 2 / 4번 맞힘 · 다음 복습일 9.18 (금)` |
| 또 틀림 | `복습 · 다시 틀려 처음으로 돌아갔습니다 · 다음 복습일 9.12 (토)` |
| 다 외움 | `복습 · 연속 4번 맞혀 오답 노트에서 뺐습니다.` |

### (2) 모의고사에서 고른 보기에 초록이 붙었다

모의고사는 **끝까지 풀고 한꺼번에 채점**한다. 그런데 고른 보기에 정답 표시용 초록 테두리가 붙어,
**고른 답이 정답이라는 뜻**으로 읽혔다.

`디자인.md` 4장에 **「아직 채점하지 않았을 때」** 절을 먼저 적고 화면을 고쳤다 —
파란 테두리 `#1B6FB8` + 오른쪽에 `고름` 글자, 초록은 쓰지 않는다.
점검표가 10줄 → **11줄**이 됐다.

### (3) 곁들여 맞춘 말

오답 노트 안내문은 `다시 틀리면 1단계로 돌아갑니다` 라는데 표는 `0 / 4` 로 적고 있었다.
표 머리를 `복습` → **`연속 맞힘`** 으로 바꾸고 안내문을 표에 맞췄다.

---

## 6. 배포본에서 직접 확인한 것

`c01d6ce` 기준. 저장소를 비우고 처음부터 돌렸다.

| 확인한 것 | 결과 |
|---|---|
| 과목 등록 | 빈 과목명·20자 미만 자료를 막고 그 칸으로 커서가 감 |
| 학습 자료 | 절 3개·용어 10개로 정리, 원문 21줄 펼치기, 출제 가능 유형 4 / 4 |
| 새로 출제 | 10문항에 **4종이 모두** — OX 3 · 객관식 3 · 주관식 2 · 빈칸 2 |
| 키보드 | `X` 키로 OX, 숫자 키로 보기 고르기 |
| 복습 주기 | 다섯 경우 전부 재현 — 처음부터 맞힘 / 새로 틀림 / 연속 1·2번 맞힘 / 되돌림 / 졸업 |
| 플래너 | 자동 나누기 4건을 9.11~9.16 에 배분, 주 이동, 9.16 에 `시험` 배지 |
| 할 일 | `2일 지남` · `오늘까지` · `마감 없음` 표기와 정렬, 엔터로 추가 |
| 학습 기록 | 14일 막대 · 자주 틀리는 절 · 회차표 |
| 모의고사 | 20문항 타이머, 이전/다음 사이 답 유지, 끝에 20줄 채점표 |
| 내보내기·불러오기 | 전부 지운 뒤 파일 하나로 **복습 단계 2/4 · 다음 9.18 까지** 복원 |
| 20문항 페이지 | 밖에서 불러오는 것 0개, 콘솔 오류 0 |
| 휴대폰 폭 375px | 전 화면 좌우 흔들림 없음. 오답표만 표 안에서 밀림 |
| **자료 전송** | 자료를 등록한 뒤 네트워크 기록에 **나간 요청 0건** (자기 파일 4개 GET 뿐) |

---

## 7. 2026-09-12 — 2일차. 같은 방식으로 다른 일을 했다

2일차 과제는 시험 대비 앱이 아니었다. **호르무즈에서 억류된 선박의 사건 보고서**였다.
그런데 **방식은 어제 것을 그대로 썼다** — 기준을 먼저 적고, 만들고, 기계가 판정하게 했다.

### 저장소에 들어온 것은 둘뿐이다

결과물(보고서 · 발표자료 · 검사기)은 **이 저장소에 없다.** 자료 폴더의 `제출/` 에 있다.
사건 자료의 회사 · 선박 · 인물은 교육용 가상이고 **외부 인용 · 공유 금지**라,
`실습파일/` 을 `.gitignore` 로 뺀 것과 같은 이유로 올리지 않았다.

바탕화면에 폴더가 두 개가 됐다. **이름이 같아서 헷갈린다.**

| 어디 | 무엇 |
|---|---|
| `바탕화면/2차_AI바이브코딩_9월11일-12일/` | 강사 자료와 2일차 제출물 |
| `바탕화면/0_[코드잇] …/2차_AI바이브코딩_9월11일-12일/` | **이 저장소.** 앱과 규칙 문서 |

### 규칙을 먼저 고쳤다 (어제 7~9장과 같은 순서)

발표 화면 규칙이 없었다. 그래서 **`디자인.md` 10장을 먼저 쓰고** 그다음 슬라이드를 만들었다.
새 색과 새 글자 크기는 만들지 않았다. 늘린 것은 **32px 을 쓰는 자리가 셋 → 넷**이 된 것 하나뿐이다.
점검표가 10장 → 11장으로 밀리고 11줄 → 12줄이 됐다.

### 만들다가 규칙을 두 번 어겼다

**지우지 않고 남긴다.**

| 어긴 것 | 어떻게 됐나 |
|---|---|
| 어두운 화면 색 네 개(`#152A20` 등)를 **지어냈다** | 1장 어두운 표에는 선·테두리 색과 연한 초록·빨강 바탕이 **아예 없었다.** 없는 값을 만드는 대신 **어두운 화면을 쓰지 않기로** 하고 10장에 그 이유를 적었다 |
| **17px 을 10장 표에 적지 않고 썼다** | 2장 문제 문장과 같은 자리라고 10장 표에 한 줄을 **추가**했다 |

둘 다 검사기가 아니라 눈으로 먼저 찾았다. 검사기는 **목록에 없는 값을 쓰는 것**은 잡지만
**목록을 잘못 적는 것**은 잡지 못한다. 규칙 문서 자체는 사람이 봐야 한다.

### 화면 버그 하나 — `[hidden]` 이 안 먹었다

슬라이드가 겹쳐 보였다. `.slide { display:flex }` 가 브라우저 기본 `[hidden]{display:none}` 을
**이겼기 때문이다.** 작성자 스타일이 브라우저 기본 스타일보다 세다.
`.slide[hidden]{display:none}` 을 직접 적어 고쳤다.

**코드만 봐서는 못 찾았다. 브라우저에 띄워 보고 찾았다** — 5장에서 배운 것과 같다.

### 검사기를 두 개 만들었다

| 파일 | 무엇을 보나 | 항목 |
|---|---|---|
| `제출/검사기.mjs` | **모양** — 장마다의 규칙 (한 장에 32px 하나, 근거 표시) | 15 |
| `제출/검사.mjs` | **내용** — 결정 6대 대상 · 타임라인 팩트 · 출처 · 스타일 | 26 |

둘 다 **기준을 베껴 적지 않는다.** 이 저장소의 `디자인.md` · `완료기준.md` 와
`실습파일/사건데이터/` 의 **실제 파일 이름**을 읽어 와서 대조한다.
바깥 요청 검사는 `scripts/build.mjs` 의 목록을 **그대로 가져왔다.**

> **첫 판에 26/26 이 나와서 그대로 믿지 않았다.**
> 사본을 일부러 망가뜨려 — `23:32` 삭제 · 「대외 발표」 절 삭제 · 출처 파일 하나 삭제 ·
> 「확인하지 못한 것」 삭제 · 없는 색 `#FF6B00` · 없는 크기 `29px` · 그림자 추가 —
> 다시 돌렸더니 **일곱 가지를 전부 잡고 `exit 1`** 이었다.
>
> **통과만 보고 넘어가면 검사기가 비어 있어도 모른다.** 새 검사기는 한 번 망가뜨려 보고 쓴다.

### 큰 파일을 어떻게 다뤘나

사건데이터는 46개 파일 · 38.6MB 인데 덩치는 **큰 표 여덟 개(약 59만 7천 행)** 에 몰려 있었다.
**대화창에 넣지 않았다.** 열 이름과 행 수만 보고, 조건에 맞는 구간을 세는 코드를 짜서 결과만 받았다.
글 32개는 다 합쳐 1,185줄이라 그냥 읽었다. 어디를 볼지는 전날 밤에 만든 `자료지도.md` 로 정했다.

**세다가 근거 하나를 버렸다.** 처음에는 「계획 항로 이탈」을 주장하려 했는데,
계산해 보니 **세 항차 모두** 항해계획서 웨이포인트에서 2.9~12.4NM 떨어져 지나갔다.
계획서 자체가 실제 항적과 맞지 않았던 것이다.
그 근거를 **쓰지 않고 버렸고, 왜 버렸는지를 보고서 8장에 적었다.**
대신 같은 배의 지난 두 항차와 직접 견주는 쪽으로 바꿨다.

### 2일차에 배운 것 한 줄

> **어제는 「모양을 규칙으로 고정한다」였고, 오늘은 「숫자의 출처를 고정한다」였다.**
> 보고서의 모든 숫자에 어느 파일에서 무슨 조건으로 셌는지를 붙였다.
> 완료기준.md 2장이 해설마다 `출처 · 파일 · 절` 을 적게 한 것과 같은 규칙이다.
> 1일차에는 출처가 **절 번호**였고, 2일차에는 **파일과 세는 조건**이 됐다.

---

## 8. 다음에 고칠 사람을 위해

### 어디를 고치면 무엇이 바뀌나

| 고칠 곳 | 바뀌는 것 |
|---|---|
| `web/index.html`, `web/app.js` | 사이트 화면과 동작 |
| `server/lib/study.js` | **출제·채점 규칙.** 사이트와 로컬 서버가 같이 쓴다 — 두 군데 고칠 일이 없다 |
| `server/public/style.css` | 모양. **고치기 전에 `디자인.md` 를 먼저 고친다** |
| `시험대비.html` | 미리 만들어 둔 20문항 (`/20.html`) |
| `scripts/build.mjs` | 빌드와 그때 하는 검사 |
| `site/` | **손대지 않는다.** 빌드가 만들어 내는 폴더다 |

### 명령

```bash
node scripts/build.mjs        # web/ 와 server/ 에서 site/ 를 만든다
node scripts/serve-site.mjs   # 만들어진 site/ 를 http://localhost:4173 에 띄운다
node server/server.js         # 프론트/백엔드를 나눈 로컬 서버 버전 (http://localhost:3000)
```

이 컴퓨터에는 `npm` 이 PATH 에 없다. `node` 는 `C:\Program Files\nodejs\node.exe` 에 있다.

### 순서를 지킬 것

1. 화면을 고치기 전에 **`디자인.md` 를 먼저 읽는다.** 없는 스타일을 새로 만들지 않는다.
2. 규칙을 바꿔야 하면 **`디자인.md` 를 먼저 고치고** 그다음 화면을 고친다.
3. 다 고친 뒤 `디자인.md` 10장 **점검표 열한 줄**로 실제 결과물을 본다.
4. push 한 뒤 **배포가 성공했는지 확인한다.** push 성공은 배포 성공이 아니다 (4장).
5. 배포된 주소를 **실제로 열어서** 한 번 돌려 본다. 5장의 두 가지는 코드만 봐서는 못 찾았다.
